/**
 * marked - a markdown parser
 * Copyright (c) 2011-2024, Christopher Jeffrey. (MIT Licensed)
 * https://github.com/markedjs/marked
 *
 * Placeholder — télécharger la vraie version depuis :
 * https://cdn.jsdelivr.net/npm/marked/marked.min.js
 */
(function(){
    // Version simplifiée pour dev — remplacer par marked.min.js en production
    window.marked = {
        parse: function(text) {
            if (!text) return '';
            // Conversion basique Markdown → HTML
            return text
                // Headers
                .replace(/^### (.+)$/gm, '<h3>$1</h3>')
                .replace(/^## (.+)$/gm, '<h2>$1</h2>')
                .replace(/^# (.+)$/gm, '<h1>$1</h1>')
                // Bold
                .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
                // Italic
                .replace(/\*(.+?)\*/g, '<em>$1</em>')
                // Code inline
                .replace(/`(.+?)`/g, '<code>$1</code>')
                // Lists
                .replace(/^- (.+)$/gm, '<li>$1</li>')
                .replace(/(<li>.*<\/li>\n?)+/g, '<ul>$&</ul>')
                // Paragraphs
                .replace(/\n\n/g, '</p><p>')
                .replace(/^(.+)$/gm, function(match) {
                    if (match.startsWith('<')) return match;
                    return match;
                });
        }
    };
})();

{
  "identifier": "default",
  "description": "Permissions par défaut pour Marianne",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-open",
    "dialog:default",
    "fs:default"
  ]
}
